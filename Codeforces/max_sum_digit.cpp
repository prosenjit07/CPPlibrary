#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long int n;
    scanf("%d", &n);
    if (n < 10)
    {
        cout << n << endl;
    }
    else
    {
        string s = to_string(n);
        int len = s.size();
        // cout<<len<<endl;
        // while (len > 0)
        // {

        // }
        


    }

    return 0;
}
